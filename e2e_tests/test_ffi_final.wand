fun test_ffi(x) { ffi!<function(n) { console.log('Hello from FFI! Number:', n); return n; }>, x };

fun get_pi(dummy) { ffi!<Math.PI> };

fun main() { 
  let result = test_ffi 42 in
  let pi = get_pi 0 in
  test_ffi pi
};